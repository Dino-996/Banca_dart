name: Console
description: Questa classe consente di effettuare la maggior parte delle operazioni di lettura con tipi di base dallo standart input.
version: 0.0.1

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  clock: ^1.1.0
  dart_console: ^1.1.2
  intl: ^0.17.0
  path: ^1.8.0